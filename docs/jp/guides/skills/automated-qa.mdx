---
title: Automated QA skill
sidebarTitle: 自動化QA
description: エンドツーエンドの自動品質保証を設定し、実際のユーザーと同様にWeb、CLI、APIの各インターフェースでアプリケーションをテストします。視覚的証拠、CI統合、障害学習機能を内蔵。
keywords:
  [
    "qa",
    "testing",
    "automated testing",
    "quality assurance",
    "agent-browser",
    "tuistory",
    "CI",
    "github actions",
    "skill",
  ]
---

コードをリリースする前に変更内容の自動テストを実行することは、最も重要なことの一つです。単体テストとリンターは構文レベルの問題を検出しますが、ログインフローが実際に動作するか、リファクタリング後にCLIが正しく表示されるか、新しいAPIエンドポイントが正しいデータを返すかどうかは分かりません。QAスキルはそのギャップを埋めます：実際のユーザーのようにアプリケーションをテストし、視覚的な証拠とともに構造化されたレポートを生成します。

Factoryは連携して動作する2つの組み込みスキルを提供しています：

- **`/install-qa`** -- コードベースを分析し、対象を絞った質問を行い、プロジェクトに合わせた完全なQAスキルを生成する一度限りのセットアップスキル。
- **`qa`** -- すべてのPRで実行される生成されたスキル。git diffを読み取り、影響を受けるアプリを特定し、関連するテストフローのみを実行します。

<Note>
  install-qaプロセスは徹底的でインタラクティブです。深いコードベース分析を実行し、多段階の質問票を実行し、複数のファイルを生成します。時間がかかり、質問が表示されることを予期してください -- 品質保証は基盤となるものであり、正しく行うために時間をかけています。
</Note>

## クイックスタート```bash
droid
> /install-qa
```このスキルは3つのフェーズで実行されます：

1. **詳細なコードベース分析** -- アプリ、技術スタック、認証、環境、機能フラグ、統合、CI/CD、既存のテストを検出します。
2. **インタラクティブな質問** -- 自動検出できなかった項目について質問します（QAターゲット、ユーザーペルソナ、重要なフロー、クリーンアップ戦略）。
3. **スキル生成** -- オーケストレーター、アプリごとのサブスキル、設定、レポートテンプレート、そして任意でGitHub Actionsワークフローを生成します。

セットアップ後は、いつでも`/qa`でQAを実行するか、生成されたワークフローを使用してPRで自動実行させることができます。

## 生成される内容```
.factory/skills/qa/
  SKILL.md                  # Orchestrator: reads diff, routes to relevant sub-skills
  config.yaml               # All env/auth/integration config (single source of truth)
  REPORT-TEMPLATE.md        # Standardized report template

.factory/skills/qa-<app-name>/
  SKILL.md                  # One sub-skill per testable app (e.g., qa-web, qa-cli)
````config.yaml` は、コードベース解析とあなたのアンケート回答に基づいて `/install-qa` によって**自動生成**されます。一度生成されれば、他のチェックインされたファイルと同じように編集できます。例：```yaml
project: MyProject
environments:
  development:
    url: https://dev.example.com
  production:
    url: https://example.com
    restrictions: [read-only only, never create data]
default_target: development
auth:
  method: email-password
  provider: WorkOS
personas:
  - name: admin
    test_focus: [settings, user-management, billing]
  - name: viewer
    test_focus: [dashboards, reports]
    cannot_do: [edit-settings, manage-users]
apps:
  web:
    path_patterns: ["apps/web/**"]
    skill: qa-web
    test_tool: agent-browser
  cli:
    path_patterns: ["apps/cli/**"]
    skill: qa-cli
    test_tool: tuistory
failure_learning: suggest_in_report
```## QAの実行方法

1. **設定の読み込み** -- 環境、ペルソナ、アプリ定義について`config.yaml`を読み込みます。
2. **差分の分析** -- `path_patterns`を使用して変更されたファイルをアプリにマッピングします。
3. **テスト実行範囲の決定** -- 影響を受けるアプリのサブスキルのみを実行します。CLIのみの変更はWebテストを完全にスキップします。
4. **テストフローの実行** -- 関連するフローを実行し、特定の差分に基づいてターゲット化されたテストを生成します。
5. **証拠の取得** -- スクリーンショット（Web）、ターミナルスナップショット（CLI）、またはAPIレスポンスデータを取得します。
6. **レポートの生成** -- 合格/不合格/ブロックされた結果を含む構造化されたレポートを生成し、PRコメントとして投稿します。

## テストツール

### Webアプリ: agent-browser

実際のブラウザを制御します -- ページをナビゲートし、フォームを入力し、ボタンをクリックし、アクセシビリティツリーのスナップショットとスクリーンショットを取得します。```bash
agent-browser open https://dev.example.com/login
agent-browser snapshot -i          # Discover interactive elements
agent-browser fill @e1 "user@example.com"
agent-browser fill @e2 "password123"
agent-browser click @e3
agent-browser screenshot result.png
```ImageMagickがインストールされている場合、QAはビジュアル回帰テストのためにUI状態のビフォー・アフターを示すアニメーション化されたGIFの差分も生成できます。

### CLI/TUIアプリ: tuistory

仮想ターミナルでアプリを起動し、キーストロークを送信して、ターミナル状態をテキストスナップショットとPNGスクリーンショットとしてキャプチャします。```bash
tuistory launch "./my-cli" -s qa-test --cols 110 --rows 36
tuistory -s qa-test wait-idle --timeout 8000
tuistory -s qa-test snapshot --trim           # Text snapshot (inline in PR comment)
tuistory -s qa-test type "/help"
tuistory -s qa-test press enter
tuistory -s qa-test screenshot --format png -o /tmp/help.png  # Visual evidence
```### API テスト

UIのないバックエンドサービスの場合、QAは標準的な`curl`コマンドを使用してエンドポイントをテストし、レスポンスを検証します。

## CI統合

プロジェクトに`.github/`ディレクトリがある場合、install-qaは以下の機能を持つGitHub Actionsワークフローの生成を提供します：

- プルリクエスト時にトリガー（Vercel/Netlifyを使用している場合はプレビューデプロイメント後も）
- ツール（tuistory、ImageMagick）をインストールし、QAスキルで`droid exec`を実行
- エビデンスをビルドアーティファクトとしてアップロードし、QAレポートをPRコメントとして投稿
- **必須**または**オプション**のチェックとして設定可能

<Warning>
  生成されたワークフローには、`config.yaml`で参照されている認証情報のGitHub secretsが必要です。install-qa
  スキルは追加すべき正確なsecretsをリストします。
</Warning>

## 失敗学習

QAが新しい失敗パターンに遭遇したとき、その知識をフィードバックできます：

| 戦略                        | 動作                                                         |
| ------------------------------- | ---------------------------------------------------------------- |
| **レポートで提案**（デフォルト） | 手動レビュー用にレポートにコピーペーストスニペットを含めます。    |
| **自動コミット**                 | 各実行後にサブスキルファイルの更新を自動的にコミットします。 |
| **PRを開く**                   | 失敗カタログの更新でドラフトPRを開きます。                   |

## 実際の例

### CLIアプリ（Go TUI）-- [glow](https://github.com/nizar-test/glow/pull/1)

ターミナルマークダウンレンダラーでヘルプテキストとフラグ説明を更新するPR。QAはGoバイナリをビルドし、tuistoryでテストしました：

| #   | テストケース                             | 結果                  | 注記                                              |
| --- | ------------------------------------- | ----------------------- | -------------------------------------------------- |
| 1   | ヘルプテキストに更新された説明が表示される   | :white_check_mark: PASS | `--help`に新しいテキストが含まれている                         |
| 2   | 行番号フラグの説明が更新される | :white_check_mark: PASS | "TUI-mode only"ではなく"rendered output"を表示 |
| 3   | CLIがマークダウンを正しくレンダリングする        | :white_check_mark: PASS | ヘッダー、リスト、コードブロックがレンダリングされる                 |
| 4   | 幅フラグが指定された列で折り返す  | :white_check_mark: PASS | `-w 40`が正しく折り返される                            |
| 5   | 標準入力パイプのレンダリング                  | :white_check_mark: PASS | パイプされたマークダウンがレンダリングされる                             |
| 6   | 存在しないファイルでのエラー             | :white_check_mark: PASS | 明確なメッセージで終了コード1                    |
| 7   | TUIブラウザの起動                    | :no_entry: BLOCKED      | CI PTY環境が一貫していない                    |

エビデンスにはインラインターミナルスナップショットが含まれています：```
$ ./glow --help
  Render markdown on the CLI, with pizzazz!
  Now with improved word wrapping and line number support.
```### フルスタックWebアプリ (FastAPI + React) -- [full-stack-fastapi-template](https://github.com/nizar-test/full-stack-fastapi-template/pull/1)

「ログイン状態を記憶」チェックボックスとフッターバージョンバッジを追加するPR。QAはCI内でPostgreSQL、Mailcatcher、FastAPIバックエンド、Reactフロントエンドを起動し、agent-browserでUIを操作しました：

| #   | テストケース                          | 結果                    | 備考                                              |
| --- | ------------------------------------- | ----------------------- | ------------------------------------------------- |
| 1   | ログインページに「記憶する」チェックボックスが表示される | :white_check_mark: PASS | フォームにはEmail、Password、チェックボックス、Log Inボタンがある |
| 2   | 「記憶する」をチェックしてログイン        | :white_check_mark: PASS | ダッシュボードにリダイレクトされる                           |
| 3   | 「記憶する」をチェックしないでログイン    | :white_check_mark: PASS | チェックしなくても動作する                              |
| 4   | 無効な認証情報（ネガティブテスト）       | :white_check_mark: PASS | トーストエラー、ログインページに留まる                       |
| 5   | フッターにv2.0が表示される            | :white_check_mark: PASS | バージョンバッジが全ページで表示される                       |

QA実行中にagent-browserが撮影した実際のスクリーンショットです：

<Frame caption="新しい「30日間記憶する」チェックボックス付きのログインページ、agent-browserが撮影">
  <img src="/images/qa-examples/qa-login-page.png" alt="記憶するチェックボックス付きのログインページ" />
</Frame>

<Frame caption="ログイン成功後のダッシュボード、v2.0フッターバッジを表示">
  <img src="/images/qa-examples/qa-dashboard.png" alt="v2.0フッター付きのログイン後ダッシュボード" />
</Frame>

## Tips

- **質問票では詳細に記述してください。** 生成されるQAスキルの品質は、提供する詳細の量に比例します。ユーザーロール、重要なフロー、認証メカニズム、エッジケースを徹底的に説明してください。install-qaに多くのコンテキストを提供するほど、生成されるテストフローはより的確になります。
- **成功基準を明確に説明してください。** 単に「ログインが動作する」と言うのではなく、「ユーザーがemailとパスワードを入力し、Sign Inをクリックし、/dashboardにリダイレクトされ、ウェルカムメッセージが表示される」と言ってください。具体性により、正しい動作を検証するテストフローが作成されます。
- **既知の癖について言及してください。** 特定のロケールでログインフォームが異なって表示される、チェックアウトに15秒かかる、開発サーバーが特定の起動コマンドを必要とする場合は、それを説明してください。これらは既知の障害モードとなり、誤った失敗を防ぎます。
- **最初の実行後に改善してください。** レポートを確認し、通過したもの、ブロックされたもの、見逃されたものに基づいてサブスキルフローを改善してください。
{/* This file is generated by `pnpm vars:build`. Do not edit directly. */}

export const vars = {
  "plans": {
    "Pro": "Pro",
    "Plus": "Plus",
    "Max": "Max",
    "Enterprise": "Enterprise"
  },
  "products": {
    "droid": "Droid",
    "droidCli": "Droid CLI",
    "factory": "Factory",
    "factoryApp": "Factory App",
    "droidExec": "Droid Exec",
    "droidShield": "Droid Shield",
    "droidShieldPlus": "Droid Shield Plus",
    "droidCore": "Droid Core",
    "factoryMissions": "Factory Missions"
  },
  "urls": {
    "factory": "https://factory.ai",
    "app": "https://app.factory.ai",
    "api": "https://api.factory.ai",
    "docs": "https://docs.factory.ai",
    "downloads": "https://downloads.factory.ai",
    "trust": "https://trust.factory.ai",
    "discord": "https://discord.gg/zuudFXxg69",
    "github": {
      "org": "https://github.com/Factory-AI",
      "repo": {
        "factory": "https://github.com/Factory-AI/factory",
        "action": "https://github.com/Factory-AI/droid-action",
        "sdkTs": "https://github.com/Factory-AI/droid-sdk-typescript",
        "sdkPy": "https://github.com/Factory-AI/droid-sdk-python",
        "plugins": "https://github.com/Factory-AI/factory-plugins",
        "eslint": "https://github.com/Factory-AI/eslint-plugin"
      }
    }
  },
  "emails": {
    "support": "support@factory.ai",
    "security": "security@factory.ai"
  },
  "install": {
    "macos": "curl -fsSL https://app.factory.ai/cli | sh",
    "windows": "irm https://app.factory.ai/cli/windows | iex",
    "npm": "npm install -g droid",
    "brew": "brew install --cask droid"
  },
  "legal": {
    "entity": "Factory AI",
    "copyright": "© 2025-2026 Factory AI. All rights reserved."
  }
};

export function ProductName({ variant }) {
  const products = {
  "droid": "Droid",
  "droidCli": "Droid CLI",
  "factory": "Factory",
  "factoryApp": "Factory App",
  "droidExec": "Droid Exec",
  "droidShield": "Droid Shield",
  "droidShieldPlus": "Droid Shield Plus",
  "droidCore": "Droid Core",
  "factoryMissions": "Factory Missions"
};

  return products[variant];
}

export function InstallCommand({ platform }) {
  const installCommands = {
  "macos": "curl -fsSL https://app.factory.ai/cli | sh",
  "windows": "irm https://app.factory.ai/cli/windows | iex",
  "npm": "npm install -g droid",
  "brew": "brew install --cask droid"
};

  return installCommands[platform];
}

export function SupportLink({ children }) {
  const supportEmail = "support@factory.ai";
  const href = `mailto:${supportEmail}`;

  return <a href={href}>{children ?? supportEmail}</a>;
}

export function SecurityLink({ children }) {
  const securityEmail = "security@factory.ai";
  const href = `mailto:${securityEmail}`;

  return <a href={href}>{children ?? securityEmail}</a>;
}

export function DiscordInvite({ children }) {
  const discordUrl = "https://discord.gg/zuudFXxg69";

  return (
    <a href={discordUrl} target="_blank" rel="noreferrer">
      {children ?? discordUrl}
    </a>
  );
}

export function DocsURL({ path = '', children }) {
  const docsUrl = "https://docs.factory.ai";
  const normalizedPath = path && path.startsWith('/') ? path : `/${path}`;
  const href = path ? `${docsUrl}${normalizedPath}` : docsUrl;

  return <a href={href}>{children ?? href}</a>;
}

export function GithubRepo({ repo, children }) {
  const githubRepos = {
  "factory": "https://github.com/Factory-AI/factory",
  "action": "https://github.com/Factory-AI/droid-action",
  "sdkTs": "https://github.com/Factory-AI/droid-sdk-typescript",
  "sdkPy": "https://github.com/Factory-AI/droid-sdk-python",
  "plugins": "https://github.com/Factory-AI/factory-plugins",
  "eslint": "https://github.com/Factory-AI/eslint-plugin"
};
  const href = githubRepos[repo];

  return (
    <a href={href} target="_blank" rel="noreferrer">
      {children ?? href}
    </a>
  );
}

{
  "name": "factory-docs-as-code",
  "private": true,
  "packageManager": "pnpm@11.1.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "PATH=/opt/homebrew/opt/node@22/bin:$PATH mintlify dev --port 3333",
    "format": "node -e \"console.log('format scaffold: no formatter configured yet')\"",
    "format:check": "node -e \"console.log('format:check scaffold: no formatter configured yet')\"",
    "lint": "pnpm format:check && pnpm vars:check",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "vars:build": "pnpm --filter @factory/docs-vars build",
    "vars:check": "pnpm --filter @factory/docs-vars check",
    "test:build": "node -e \"console.log('test:build scaffold: Mintlify build validation is wired in M4')\"",
    "prepare": "node -e \"process.exit(0)\""
  }
}

# optional server settings (bun auto-loads .env)
# copy to `.env` and tweak as needed

# server bind
# PORT=4000
# HOST=localhost

# droid exec tuning
# DROID_MODEL_ID=gpt-5-codex
# DROID_REASONING=low  # off|low|medium|high
